server:
  port: 8087
  address: 0.0.0.0
  servlet:
    context-path: /api

spring:
  application:
    name: component-service

component:
  storage:
    base-dir: Userdata
  llm:
    base-url: http://localhost:8082

# 业务阶段：调用 llm-service、组件 JSON/HTML 存储策略等
